services:
  xgc2-stt-runtime:
    image: ghcr.io/xgc-team/xgc2-images/xgc2-stt-runtime:1.0.0
    container_name: ${CONTAINER_NAME}
    working_dir: /work
    volumes:
      - ${PWD}:/work
    stdin_open: true
    tty: true
    command: ["bash"]
    healthcheck:
      test: ["CMD", "/usr/local/bin/xgc2-stt-runtime-healthcheck"]
      interval: 30s
      timeout: 15s
      retries: 2
