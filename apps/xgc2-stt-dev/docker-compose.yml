services:
  xgc2-stt-dev:
    image: ghcr.io/xgc-team/xgc2-images/xgc2-stt-dev:1.0.0
    container_name: ${CONTAINER_NAME}
    working_dir: /work
    volumes:
      - ${PWD}:/work
    stdin_open: true
    tty: true
    command: ["bash"]
    healthcheck:
      test: ["CMD", "/usr/local/bin/xgc2-stt-dev-healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 2
